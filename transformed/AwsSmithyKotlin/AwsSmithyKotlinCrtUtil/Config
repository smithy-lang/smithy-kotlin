# -*-perl-*-

package.AwsSmithyKotlinCrtUtil = {
    interfaces = (1.0.x);

    third-party-configuration = {
        thirdPartyName    = "aws.smithy.kotlin:crt-util-jvm";
        thirdPartyVersion = "1.6.10";
        licenseType       = (Apache-2.0);
        licenseFile       = (https://www.apache.org/licenses/LICENSE-2.0.txt);
        repository        = maven;
        downloadType      = binaries;
    };

    build-system = copysrc;

    build-tools = {
        1.0.x = {
            CopySrcBuild = 1.0;
        };
    };

    dependencies = {
        1.0.x = {
            AwsSmithyKotlinRuntimeCore = 1.0.x;
            AwsCrtKotlin               = 0.12.x;
            AwsSmithyKotlinHttp        = 1.0.x;
            KotlinStdlib               = 2.x;
        };
    };

    runtime-dependencies = {
        1.0.x = {
            KotlinxCoroutinesCoreJvm = 1.x;
        };
    };

    resolves-conflict-dependencies = {
        1.0.x = {
            KotlinxCoroutinesCoreJvm = 1.x;
        };
    };

    targets = {
        crt-util-jvm = { type = javalibrary; };
    };
};
